Generate HTML that visually matches the document image.

Constraints:
- Final render size must be exactly <WIDTH>×<HEIGHT> px.
- Preserve the same columns and visual structure.
- Use fixed pixel sizes for layout where needed.
- Do NOT add bounding boxes or layout metadata.

Images:
- Use fixed-size placeholders for images using provided image_sources and their width and height attributes.
<IMAGE_PLACEHOLDER>

Text:
- Match font style and spacing as closely as possible.

Output:
- Single self-contained HTML with inline CSS only.
- No explanations.
